# env_file: ".env"

services:
  name:
    cmd: "echo 'hi'"
  compose:
    cmd: "docker compose -f services/docker-compose.yaml up"
  one:
    cmd: "cd services/one && go run ."
  two:
    cmd: "cd services/two && go run ."
    wait_for:
      - "localhost:3000"
      - "amqp://dev:dev@127.0.0.1:5672"
      - "postgres://postgres:dev@localhost:5432/?sslmode=disable"
